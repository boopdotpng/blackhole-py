# Microbenching TODO

Single merged list + measured-constants reference (2026-06-10).

## State of knowledge — measured constants

Aggregated from every doc under `docs/`; details + raw runs in the source docs.
Cycles at the AICLK shown in source doc (800 idle / 1350 boost — see ★★ normalize item below).

### RISC-V cores (all five behave identically)

| quantity | value |
|---|---:|
| base ALU/imm/shift/compare/branch/jal | 1.0 cyc/op, no branch penalty either direction |
| `mul` dep / indep | 1.75 / 1.0 |
| `divu`/`remu` dep | ~6.0 |
| `fence` | 4.0 |
| CSR read (`csrrs/c`) | 4.0 |
| local LDM load/store; LDM ptr-chase | 1.0 |
| LDM store→load same addr | 1.5 (TRISC2: 2.0 — anomaly) |
| L1 lw fixed / ind4 / ind8 | 2.6 / 1.25 / ~1.5 |
| L1 dependent load / ptr-chase | 5.0 (NCRISC 5.165 — anomaly) |
| L1 load-use total (0/1/2/4 fillers) | ~9 cyc total — fillers hide free |
| L1 store; store→load same addr | 1.0; 6.5/pair |
| L1 same-addr RMW, solo→all-five | 8 → 14.5 (B/N/T0) / 10.5 (T1/T2 — asymmetry) |
| MMIO read (wall clock, NoC status) | 2.0 (2.25 5-way contention) |
| cross-LDM read solo / pair / 5-way | 2.5 / 5 / 17 — shared interconnect, not per-window port |
| cross-LDM write port | separate; writer ~1, helps readers (17 → 13) |
| counted-loop overhead | 3 cyc/iter |
| wall-clock skew between cores | constant per pair, zero span — usable for deltas |

### NoC

| quantity | value |
|---|---:|
| cmd issue (read/write) | ~23–25 cyc |
| local read flush / write barrier | ~105 / ~95 cyc (smoke); 79/69 adj at 100 iters |
| peer flush/barrier (any dx 1–13) | ~230/225 cyc — **flat in hop count**, NIU-dominated |
| single-stream saturated stream | ~63–64 B/cyc per direction, flat everywhere |
| max burst | 16 KiB (256 × 64-byte NoC words) |
| aggregate 60 disjoint pairs | 3452 (NoC0) / 3464 (NoC1) B/cyc ≈ 4.7 TB/s per NoC |
| 1 RISC driving both NoCs / dual-DM (B+N) | ~962 / ~961 B/cyc — issue/L1-bound, NoC1 misrouted |
| topology | torus wrap confirmed (ratio 0.992–1.0); **X-then-Y** routing confirmed |
| arbitration | only adjacent-injector hurts victim (0.736×); rest ~baseline |
| VC isolation | none observed — reads 1.43–1.48× slower under write saturation, all VC fields |
| bisection (4 pairs) | 1.585× single link — endpoint-bound, not fabric |
| mcast (unlinked, 3 dests) | NoC0 row 351, col 462; NoC1 row 533 (1.5× anomaly), col 462 cyc/16 KiB |
| sem mcast set / atomic inc+ack | ~103 / ~214 cyc |
| write ack vs receiver-visible | ack conservative by ~140–150 cyc (NoC0), ~0 (NoC1) |

### DRAM / GDDR

| quantity | value |
|---|---:|
| 1 core, standard / stateful | 36–37 / 62–78 GB/s |
| controller roof | ~60–80 GB/s regardless of endpoint/mode |
| endpoints ×3/controller | routing convenience, not 3× bandwidth |
| chip read peak | 302 GB/s (NoC0, 16 KiB, 118c); 376 @ 56c then drop (anomaly) |
| chip write peak | 305 GB/s (NoC1, split3) |
| theoretical | ≥448 GB/s — ~140 GB/s unexplained gap |
| DRISC GDDR DMA | 59–64 GB/s 1 stream; 2 KiB tile 209 read / 103 write cyc |
| worker stateful 2 KiB write | 37.5 cyc/tile (74 GB/s) |

### Tensix backend

| quantity | value |
|---|---:|
| FIFO push; STALLWAIT push | 1.0; 1.54 cyc |
| PC_BUF_SYNC drain edge | ~8–12 cyc |
| math init / MOP program | 266–285 / 100–110 cyc one-time |
| tile-K (unthrottled/throttle0) | 54 / 49 cyc; FPU 16 cyc/tile (8×16×16 ≈ 2048 MAC/cyc) confirmed by 300 TFLOP runs |
| MVMUL scaffold tile latency / 2×2 | 90 / 187 cyc; throughput 147 / 244 |
| unpack row / 2×2 bw4 / bw6 / reload | 37.5 / 300 / 487 / 274 cyc |
| pack | **contradiction**: 924 whole-kernel fit vs 5265–7425 profiled — #1 unexplained |
| SFPU 32-lane group | exp 40, recip 16, rsqrt 58, sigmoid 59, silu 62, sum32/max 51 (issue-bound), rowmax 293/tile |
| eltwise binary/bcast | ~1234 cyc/tile = single-core feed bound; math hidden |
| softmax composite | 2738/tile = sum-of-parts, no composition surprise |
| sem/CB | CB wait/reserve ~22/23, push/pop 31/41.5 (tensix), TTSEM 2.3–8 |
| XMOV DMA-regs | SET/RST/NOP ~1–1.25, SHIFT ~3 steady; movers A2D/D2B QUARANTINED |
| GEMV M=1 | 76–96 GB/s = 30 % of roof; 1 block in flight/reader |

Verilog readiness: RISC ~90 %, NoC ~55 %, Tensix ~50 %, DRAM ~40 %. Biggest
unknowns: router internals, MOP expander, pack pipeline, GDDR controller
geometry, CQ dispatch FW. For the static model the gap is queue depths +
contention laws + blocking-release latency + launch constant, not more
single-stream numbers.

## Open benches

Status:
**OPEN** = not started · **GATED** = waiting on validation/device · **QUARANTINED** = wedge risk · **MODEL** = no device needed.
Priority: ★★★ blocks both goals (Verilog clone + static timing model), ★★ blocks one, ★ refinement.

## NoC

- [ ] ★★★ **Per-hop router latency — single biggest hole.** Hop sweep flat (~230 cyc fixed NIU dominates). Restart pointer-chase with one tiny K=2 chase, short timeout, queue-only. Slope = router pipeline depth; needed for any routing inference. GATED (prior chases timed out)
- [ ] ★★★ **Injection-rate / outstanding-depth sweep.** 1 sender, outstanding 1→64 vs achieved BW; gives NIU queue depth and the latency–bandwidth law `BW = outstanding × 64 / (230 + hops)`. OPEN
- [ ] ★★★ **Arbitration full matrix (Exp A).** Gated until K=2 smoke shows bad counters=0 AND bad sentinels=0 (always 2 — likely host-readback bug). Debug sentinel encoding first. GATED
- [ ] ★★ **Bisection at scale (Exp F).** 4 pairs gave 1.585× (NIU-bound). All rows, both directions, NoC0+1 → links × width across midline. OPEN
- [ ] ★★ **Dual-NoC aggregate ceiling.** 60-pair dual-DM ~961 vs 3450 single; NoC1 direction was flipped. Rerun direction-correct; prove/refute 2× fabric. Moderate/full same-core sweeps QUARANTINED (PCIe wedge)
- [ ] ★★ **Mcast NoC1 row anomaly + dest scaling.** NoC1 row 1.5–1.6× slower; sweep dests 1→12 × direction → mcast tree cost(dests). Linked mcast excluded (hang). OPEN
- [ ] ★★ **Read/write shared-resource isolation.** Reads 1.43–1.48× slower under writes in all VC configs — try disjoint src/dst tiles, smaller writes to find NIU vs router vs L1 ports. OPEN
- [ ] ★★ **Bidirectional ceiling per receiver.** Max into one tile / one column not measured. OPEN
- [ ] ★ **Logical→physical coordinate map.** x=8,9 gap + harvest rows unconfirmed; topology probe + wrap timing. OPEN
- [ ] ★ **Posted vs nonposted + outstanding-write credit limit.** Credit logic for clone. OPEN
- [ ] ★ **Router/NIU cfg register dump spike** (`ROUTER_CFG_0`, `NIU_CFG_0`) — ground truth vs behavioral inference. OPEN
- [ ] ★ **Linked mcast / path reservation** — sim first, then one quarantine-grade probe; required for clone. OPEN

## DRAM

- [ ] ★★★ **Chase GDDR roof.** Port DRISC `gddr_dma.h` (255 outstanding reads, 262 kB, 2 streams, ×7 controllers); needs DRAM-core/DRISC launch in blackhole-py. ~305 vs ≥448 GB/s. Includes dual-stream microkernel (never valid) and overlap modes (`dma2/serial/pipe/pipe2` timed out). OPEN — biggest BW upside
- [ ] ★★ **Per-controller verification.** 7 disjoint controller×core sets simultaneous: 7×~65 ≈ 455 vs ~300 observed. OPEN
- [ ] ★★ **Non-monotonic core scaling.** 376 GB/s @56 → ~300 @118; fine sweep for contention point. OPEN
- [ ] ★★ **Bank/row pattern sweep.** Stride / same-row vs row-thrash → controller geometry, BW = f(pattern). OPEN
- [ ] ★★ **Read-latency stride sweep.** Open vs closed row → tRC/tRCD analogs for clone. OPEN
- [ ] ★ **Read/write turnaround mix.** Bus turnaround penalty. OPEN

## Tensix backend

- [ ] ★★★ **Pack isolation.** 924 fitted vs 5265–7425 profiled — 6–8×, #1 unexplained pipeline number. Needs unpack/math companion (standalone hangs); prerequisite: dest readback. GATED
- [ ] ★★★ **MVMUL 2× gap + 16-cyc claim.** 16 encoded vs 8 arch slots; multi-body single-drain burst to expose retirement rate. OPEN
- [ ] ★★ **TTMOP/replay expander cost model.** Size sweep → issue amplification; also unblocks static binary→trace lowering. OPEN
- [ ] ★★ **Fidelity/dtype sweep.** HiFi2/4 + bfp8/bfp4 (GEMV follow-up) → dtype scaling constants. OPEN
- [ ] ★★ **Multi-subblock steady state in one launch.** Math hangs without TRISC0; minimal triangle scaffold. OPEN
- [ ] ★★ **Blocking-wait release latency** (TTSEMWAIT, CB waits) with known release timing — last handshake gap for the model. OPEN
- [ ] ★★ **Math-isolated eltwise throughput** (no DRAM-feed bound, no-pack drain); HiFi2 mul. OPEN
- [ ] ★ **Dest readback minimal re-enable** — 64-row walk only; no-readback control → readback → re-validate pack. Gates pack isolation + SFPU validation. QUARANTINED (checklist in docs/tensix/dest-readback.md)
- [ ] ★ **XMOV movers** TTMOVA2D/D2A/D2B + debug moves — sim first. QUARANTINED; DMA-reg family done
- [ ] ★ **Dest/SrcA/SrcB bank conflicts + double-buffer flip cost** — regfile structure for clone. OPEN
- [ ] ★ **Unpack BW vs format/stride; THCON descriptor sweep** — unpacker datapath width. OPEN

## RISC-V + dispatch

- [ ] ★★ **Launch/dispatch overhead** vs core count / binary size; CQ incremental cost — the constant term in every kernel prediction. OPEN
- [ ] ★ All-pairs contention matrix (`--all-pairs`, 16 groups); cross-LDM interconnect topology (5×5 under load). OPEN
- [ ] ★ NCRISC L1-load anomaly (5.165; ind4 1.292) repeat across tiles; TRISC2 LDM pair 2.0 vs 1.5; RMW asymmetry (~14.5 vs ~10.5) — port priority? OPEN
- [ ] ★ IRAM vs L1 execution; I-fetch contention vs other L1 ports — L1 bank map for clone. OPEN

## Model / no device needed

- [ ] ★★★ Resource-queue overlap model (RISC, NoC0/1, DRAM ctrl, unpack/math/pack, L1 ports, sems) in program_timing_model. MODEL
- [ ] ★★★ Close 0.42 vs 24.88 TFLOP/s gap budget at 384³ from clean constants; predict 5000³. MODEL
- [ ] ★★ Normalize all docs to cycles (800 vs 1350 MHz mismatches). MODEL
- [ ] ★★ Static binary→cycle-trace lowering (decode TTMOP/replay statically; feeds kernel search). MODEL
- [ ] ★ Dedupe noc_topology helper vs probe fallback (`shared_links` API). MODEL

## Done log

- [x] **SFPU timing (exp, rsqrt, recip, sigmoid, silu).** DONE 2026-06-09 via `microbench_sfpu_transcendental.py --iters 4096` (issue-side wall-clock loop on TRISC1, no dest-readback needed). bf16, 32-lane group: exp 40 cyc, recip 16, rsqrt(5 Newton) 58, sigmoid 59, silu 62 → ~1.25–1.94 cyc/elem (~1.3–2.0k cyc/tile). All five validated vs numpy (max_rel ≤0.74%). llama swiglu cost ≈16k cyc/token — negligible vs DRAM bound.
- [x] **SFPU transcendental harness** — first hardware runs 2026-06-09, correctness PASS jobs `0162bd90`, `08553072`; timing `2410fbae`.
- [x] **SFPU reduce (sum/max/row-max).** DONE 2026-06-09 `microbench_sfpu_reduce.py --iters 4096` (jobs `1144380b`, `44766763`/`4dd826ce`, repeatable <0.03%). bf16: sum32 51 cyc/group (1.59 cyc/elem), max8x4 51, rowmax 293 cyc/tile. sum32==max8x4 despite 33 vs 27 instrs → RISC issue-bound. Contracts pinned: horizontal max per-8-lane row (lane 0), reduce_row_max_tile clobbers odd cols, only col0 valid.
- [x] **Eltwise binary (add/sub/mul) + broadcast (row/col/scalar).** DONE 2026-06-09 (jobs `b7e6f3bd`/`a8d3cc01`, `cf09dee8`/`a94e407c`). First two-operand pipeline (CB0→SrcA, CB1→SrcB). 9/9 PASS; mul LoFi ~3% low. ~1234 cyc/tile all variants = single-core DRAM-feed-bound; math hidden. Gotcha: SYNC_L1 +0x10000 collided with 3rd CB data (moved +0x20000).
- [x] **Softmax composite.** DONE 2026-06-09 (jobs `b6fe7f37`, `9c452b63`×2, 0.002%). 2738 cyc/tile = 85.6 cyc/row, math-bound, ≈ sum of parts — no composition surprise. Bands looped with offset-patched SFPLOAD/SFPSTORE (3.6 KB IRAM).
- [x] **cb_reserve_back never blocks when CB full.** FIXED 2026-06-09. `cb_pop_front(tensix_ack=True)` emitted both deferred Tensix ack AND eager RISC `sw acked` → pages freed at issue rate, BRISC lapped CB0. Fix in ttk/cb.py: eager publish gated on `not tensix_ack`/`not tensix_received`. A/B: softmax 64-tile FAIL→PASS (`54ab77b1`→`ee153e6f`), flow control adds no overhead. Regressions PASS incl. matmul_peak 18.07 TFLOP/s.
- [x] **Skinny GEMV (M=1).** DONE 2026-06-09 (jobs `a9bdd139` … `2e43df82`). bf16 llama-1B shapes: kv 76 / q 87 / gate 90 / down 96 / logits 94 GB/s ≈ 30% of 305 roof → ~36 tok/s. Bottleneck: 11 NCRISC readers, 1 block in flight. **CQ wedge ROOT-CAUSED+FIXED:** prefetch split multi-page records across 128-page dispatch-CB wrap; dispatch parsed stale L1 → corruption + credit deadlock. Fix in cq.py `_issue_write`: pad to ring boundary + pad to 64 MiB end; ring 512K→1.25M. ~660 differing launches zero hangs. **Relaunch corruption SEPARATE bug, FIXED:** TRISC fw only reset state at boot; firmware now resets regfile/state every RunSync.GO. 400-launch soak PASS, fails=0.

- [ ] ★★★ **On-device tilize via unpacker tileize_mode (llama plan #8).** IN PROGRESS 2026-06-10, FAILING (`microbenching/tensix/microbench_tilize.py`, jobs 75147e60→cc43064d). Pinned facts: (1) Blackhole REG2 Shift_amount is 4-bit nibbles at bits 16/20/24/28; ttsim (`tensix.cpp:2432`) says RowStride bytes = (c0<<4)|(c1<<8)|(c2<<12) but HW measures ~16× that — value 0x0001 gives the correct 256 B row pitch for a 32x128 block (rows 0/1 land perfectly). (2) tileize_mode strides per 16-datum chunk so ONE UNPACR covers only a 16-col strip — cols 16-31 of each tile need per-face base offsets (+32 B, +16·pitch) like llk_unpack_tilizeA_B. (3) SETADCXX(1,1023) sets ch0=0/ch1=1023 (count is fine); forcing REG2_1 x-count 1023 kills the stream. (4) Remaining failure: a deterministic interleave permutation + duplicate chunk reads vs the math/pack dest mapping; per-face UNPACR variant (4 dvalids/tile, add1 math mop) hangs flow control. Next: dump SrcA directly after one tilize-UNPACR (dest readback bench) to separate unpack order from math/pack mapping.
- [x] **Embedding gather (llama plan #7, read half).** DONE 2026-06-10 (jobs `2673aa58` 1k rows, `db07cbca` 16k rows / 1024 toks, `d99c84e6` full vocab 128256 rows = 525 MB table; all byte-exact). NCRISC-only kernel `microbenching/noc/microbench_embedding_gather.py`: stage token-id pages → L1, per token `lw` the id from L1, page = id·pages_per_row into the standard `dram_tile_addr_from` + 2× 2 KiB `noc_read` → L1 → 2× `noc_write` to out slot. Table row-major in the default 2 KiB-page interleave (one row = exactly 2 pages at dim=2048); host `Allocator` reused unchanged. First kernel where the tile id comes from data instead of RTA+counter — the missing `embed_kernel` primitive. Note: tied logits GEMV needs a second *tilized* table copy (525 MB extra, fine).
- [x] **Embedding gather directly to GEMV input layout.** DONE 2026-06-10 (`microbenching/noc/microbench_embedding_gather_tilized.py`, jobs `e0b7cc3e` 64×1024 smoke, `abe53f03` 4096×2048, `8a5dbb3a` full 128256×2048). NCRISC gathers row-major bf16 source rows by data-loaded token id, then writes only row 0 into each 32×dim tilized M=1 GEMV input slot; caller pre-zeros padded rows. Key gotcha: `dram_tile_addr_from` clobbers `t0..t2`, so output tile page must live in an `s` reg across the two row-half writes. This is the useful embed bridge for `examples/llama3.py`; the older row-major gather is still the raw CUDA-layout copy primitive.
- [x] **Fused embedding gather + skinny GEMV launch.** DONE 2026-06-10 (`microbenching/matmul/microbench_embed_gather_gemv.py`, jobs `e1222fff` 2048×256, `f3154889` 2048×512, `f6842bcc` QKV-sized 2048×3072, `b5bdcceb` FFN-sized 2048×8192, `a413ed1c` QKV-sized 100-launch soak). BRISC sender preamble loads token id from DRAM, gathers row-major embedding into row 0 of the tilized M=1 A buffer, then releases the normal matmul TRISCs in the same launch. This proves the host `embed_row()` + host A upload can disappear without adding a launch. Gotchas: CQ uniform writes required padding the receiver BRISC text and RTA blob; NoC helpers clobber temp regs, so never pass the L1 src/dst register as `a`/`v`; gather only logical-K tiles, not padded K tiles, so padded columns stay zero; scratch must be placed after the matmul plan's CB footprint (fixed after QKV-sized run exposed overlap with CB1 at col 1024).
- [x] **Embedding gather into staged RMSNorm inputs.** DONE 2026-06-10 (`microbenching/noc/microbench_embedding_gather_norm_inputs.py`, jobs `68083aaf`, `a2d9c184`, `9740fb94`, `005adf85`, cleanup rerun `b17c69ee`, current rerun `a1e59548`). Two-launch staged bridge: gather token id from device memory into the row-tiled M=1 input layout, then convert row tiles into RMS SFPU footprint tiles using the validated row-to-footprint converter. Llama-width one-token, 8-token, full-vocab 128256×2048, and 100-run soak all PASS with both row layout and RMS footprint layout byte-exact. Integrated into `examples/llama3.py` as `DeviceTokenEmbeddingNormInputs` for layer-0 QKV's staged RMSNorm inputs: one-layer checked decode PASS (`73ce67e1`, logits PCC 0.41452, argmax ok, 32 launches/token), full 16-layer one-token PASS (`c292d5e3`, 347 launches/token, 0.97 tok/s), full 16-layer four-token survived (`359fdc3b`, 0.96 tok/s). This proves token->embedding can feed both `DeviceNormGemv.x_row_buf` and `x_rms_buf` without host materialization for the device QKV path. Host still keeps an embedding copy while attention/residual fallbacks need it. Lesson: a one-launch row-major gather + RISC lane-extract path was dropped because raw row-major NoC L1 buffers are safe for NoC copies but not a good source for RISC halfword lane extraction; use row-tile layout then convert.
- [x] **Norm-scale producer into skinny GEMV input.** DONE 2026-06-10 (`microbenching/matmul/microbench_normscale_gemv.py`, jobs `ad946ad0` QKV-sized 2048×3072 and `24d91d13` 100-run relaunch soak). Device row-broadcast mul writes `x * norm_scale` into the same padded, tilized M=1 A-buffer layout consumed by skinny GEMV; A-buffer validation PASS, GEMV PCC=0.999963/rel_l2=0.010763. This proves the host `normed()` materialization/upload boundary can disappear. This older bridge still uses a host-supplied `norm_scale = norm_w * inv_rms`; the staged device RMSNorm bridge below removes that host calculation.
- [x] **Device RMSNorm inverse scalar.** DONE 2026-06-10 (`microbenching/tensix/microbench_rmsnorm_inv.py`, jobs `67eb57de` K=2048 smoke and `2164b729` 100-run soak). Three staged device reductions compute `inv_rms = rsqrt(mean(x*x)+eps)` without host arithmetic: 64 sumsq partials, 2 partial sums, final scale+eps+rsqrt. Result rel error 0.000303 vs bf16 host reference; 300 launches in the soak stayed stable. This removes the host scalar calculation from RMSNorm, and the staged bridge below feeds that scalar into device norm-weight/activation scaling. Gotcha: compact-writer lane-index math must not reuse the same register for the lane and byte offset; doing so scattered partials to wrong footprint slots and produced a too-large inverse.
- [x] **Staged device RMSNorm scale into GEMV.** DONE 2026-06-10 (`microbenching/matmul/microbench_rmsnorm_scale_gemv.py`, jobs `bd36739a` QKV-sized 2048×3072 smoke and `ed6e6de5` 100-run soak). Connects device `inv_rms` reduction to device row-bcast `x * norm_w`, device scalar-bcast `* inv_rms` into the padded tilized M=1 A buffer, then skinny GEMV. A buffer PASS, GEMV PCC=0.999951/rel_l2=0.011411, and the soak covered 600 launches. `examples/llama3.py` now uses staged `DeviceNormGemv` for QKV, Wgate/Wup, and tied logits in device mode: class smoke PASS (`7e5f873f`, PCC=0.999771/rel_l2=0.043849), 100-run wrapper soak PASS (`e310a033`, 600 launches), and one-layer driver survives (`4828bf59`, argmax ok, 33 launches/token). This removes integrated host `rmsnorm_inv()`/`normed()` arithmetic, but numerical fidelity is not solved: current stable default logits PCC is only 0.42827 vs the fp32 host golden. Scaled-square RMS experiment: unscaled token-791 device `inv_rms` was 7.53125 vs host 59.53367 (`553a658f`); scale-16 fixed the model row (`ebfee07d`, constructor knob `ca9d9e8a`) and lifted one-layer logits PCC to 0.99803 (`27545113`, `c3323f24`), but scale 16/12/10/8 produced non-finite output on random large-activation tests (`e5717bd1`, `224b8b58`, `c8aa97c5`, `45819303`, `dbeeb9e1`). Default remains unscaled for stability (`b6e2f354`). Next correctness target is bounded/adaptive device-side RMS scaling or fp32/higher-fidelity norm scaling, before launch fusion. Gotcha: existing bcast feeder assumed interleaved A/B pages, so the bench adds a two-source BRISC feeder that reads A and B from separate device buffers, with optional fixed B tile for scalar broadcast. Supersedes the earlier scalar-first `microbenching/matmul/microbench_rmsnorm_gemv.py` proof.
- [x] **Projection GEMV + residual add.** DONE 2026-06-10 (`microbenching/matmul/microbench_residual_gemv.py`, jobs `6a2b9170` wo-shaped 2048×2048, `bfd201ea` wdown-shaped 8192×2048, `ebde4d2e` wdown 100-run soak). Stages projection GEMV output in DRAM, then a separate two-source eltwise ADD program streams row-0 output tiles plus the resident residual row and writes the next activation buffer. GEMV validates (wdown PCC=0.999950/rel_l2=0.012634), residual add PASS, and the soak covered 200 launches. `examples/llama3.py` now uses this as `DeviceResidualGemv` for Wo/Wdown in device mode; class smoke PASS (`5dc890b4`) and residual-only one-layer decode `--check` PASS (`b6972096`, logits PCC 0.99982, argmax ok). This removes the host `x = x + projection(...)` arithmetic from the integrated path without forcing a mega-kernel. Historical note: early integration read the post-add activation back while attention was still host-side; the later resident handoff entry below keeps it on device in `--device-attention` mode. Wdown can also run from a device-prefilled input buffer produced by staged SwiGLU.
- [x] **QKV GEMV + V-cache scatter tail.** DONE 2026-06-10 (`microbenching/matmul/microbench_qkv_vscatter_gemv.py`, jobs `6567db6b` QKV-sized 2048×3072 and `82d78c68` 100-run relaunch soak). Added an optional `matmul_peak` NCRISC output-tile hook: while the QKV output tile is still in CB16, the hook copies V-slice row 0 into `v_cache[head,pos]` with two 32 B writes per output tile. GEMV remains clean (PCC=0.999941/rel_l2=0.010788), V cache row byte-exact, untouched rows zero. `examples/llama3.py` now uses this hook in the device QKV wrapper and maintains a per-layer device V cache in parallel with the host fallback cache. This fuses the easy half of KV append into QKV; K still needs RoPE before scatter. Gotchas: output writer has already repurposed `s11`, so extra RTAs need an explicit temporary RTA pointer; `rta_ptr` and DRAM address helpers clobber `t0..t2`, so keep data-loaded tile indices elsewhere across them; restore the normal 2 KiB output-write NoC state after the 32 B scatter writes.
- [x] **RoPE + K-cache scatter composite.** DONE 2026-06-10 (`microbenching/tensix/microbench_rope_k_scatter.py`, jobs `5e5a2630` 8-head Llama-1B K shape and `83b14856` 100-run relaunch soak). Single-core add1-style Tensix pipeline stages one K head plus cos/sin as four SFPU footprints in one tile, runs `ttk.sfpu.rope_rotate_row_seq`, then NCRISC compacts rotated x1/x2 footprint lanes into `k_cache[head,pos]`. K cache row validates, untouched positions stay zero, launch avg ~14.7 us for 8 heads. This is the K-side composite boundary for now; K cannot use the raw QKV tail because it must be rotated first. Gotcha: staged footprint offsets `0/4/8/12` map cleanly to x1/x2/cos/sin row groups inside one tile, and the writer compacts sparse packed-tile lanes into a contiguous 64-element cache row.
- [x] **QKV K-output stage into RoPE+K scatter.** DONE 2026-06-10 (`microbenching/matmul/microbench_qkv_kstage_rope_scatter.py`, split-path jobs `98f63391` 8-head smoke and `bccdc78d` 100-run soak; combined current job `101a751a`). BRISC/NOC staging reads K tiles from the normal QKV GEMV C-buffer layout, reads device cos/sin row tiles for `pos`, writes the sparse x1/x2/cos/sin SFPU footprint consumed by `microbench_rope_k_scatter.py`, then runs the RoPE+K-cache scatter program. The current default fuses that staging as a BRISC preamble to the RoPE program, so K update is one launch after QKV instead of a separate stage launch plus RoPE launch. Combined path: RoPE source footprint PASS, K cache row PASS, untouched rows PASS. Integrated into `examples/llama3.py` as `DeviceKCacheUpdater` with one `k_stage_rope_scatter` program while host attention remains a fallback; one-layer driver `--check` PASS (`05d258db`, 31 launches/token, logits PCC 0.41452, argmax ok). Full 16-layer one-token decode survived (`7d6f0a4d`) at 0.97 tok/s, 361 launches/token, ~61.5 ms/token device time. This is the intended boundary for now: QKV GEMV, K-stage+RoPE scatter, and later attention remain separate programs.
- [x] **Device SwiGLU bridge + driver integration.** DONE 2026-06-10 (`microbenching/matmul/microbench_swiglu_bridge.py`, original staged jobs `51f26e33`/`43e1b4f5` hidden=8192 smoke and `d8eba984`/`360d6696` 100-run soak; fused row->SiLU job `fcefdcbe`). Converts Wgate row-tiled GEMV output to the SFPU footprint layout, runs SiLU, converts back to row tiles, then multiplies by Wup row tiles into Wdown's M=1 input buffer. The current default fuses row->footprint as a BRISC feeder into the SiLU program, then keeps footprint->row and row-tiled multiply as separate programs: hidden=8192 PASS (`fcefdcbe`, 3 launches, row->footprint PASS, SiLU PASS, SwiGLU row PASS). Integrated as `DeviceSwiGLU` in `examples/llama3.py`: Wgate/Wup run without host readback, staged/fused SwiGLU fills Wdown's input buffer, and Wdown runs via `DeviceResidualGemv.run_prefilled()`. One-layer checked decode PASS (`6a81ce3e`, 30 launches/token, argmax ok); full 16-layer one-token decode PASS (`986d7f3e`, 345 launches/token, 0.96 tok/s); full 16-layer 4-token decode survived (`68b7ff64`, 0.96 tok/s). This removes integrated host `swiglu(gate, up)` arithmetic/readback and cuts SwiGLU from 4 launches/layer to 3. Gotcha: a more aggressive direct row->SiLU->row program passed one tile but failed multi-tile validation when NCRISC read packed output directly from CB16; use the raw NoC write path plus separate footprint->row conversion for stability.
- [x] **Shared FFN RMSNorm for Wgate/Wup in driver.** DONE 2026-06-10 (`examples/llama3.py`, jobs `257cb9fd`, `605c7231`, `f3b53bb8`). Wgate and Wup use the same `ffn_norm(x)`, so Wgate's `DeviceNormGemv` now produces the normalized GEMV A buffer once, Wup runs only its resident-weight GEMV from that prefilled buffer, and staged `DeviceSwiGLU` consumes both C buffers. One-layer three-token checked decode PASS (`257cb9fd`, 32 launches/token, logits PCC 0.41452..0.44233, argmax ok, 9.23 tok/s). Full 16-layer one-token launch breakdown PASS (`605c7231`): 377 launches/token at 0.97 tok/s, with RMS reductions 99, norm-scale bcasts 66, GEMV 84, SwiGLU 64, residual adds 32, K-stage 16, K RoPE-scatter 16. Full 16-layer four-token decode survived (`f3b53bb8`) at 0.96 tok/s. This removes 80 launches/token versus the staged-SwiGLU path without adding a new kernel. Caveat: a first four-token one-layer checked run (`56f56c89`) got through three correct tokens at 32 launches/token, then CQ timed out during a host DRAM fill; queue reset + rerun passed.
- [x] **Llama driver launch accounting.** DONE 2026-06-10 (`examples/llama3.py --launch-breakdown`, jobs `7c42e7a8`, `f691bde1`, `4b5ff0aa`, `605c7231`, `7d6f0a4d`, `986d7f3e`, `c292d5e3`). Before integrated SwiGLU, full 16-layer decode was 393 launches/token: RMS reductions 147, norm-scale bcasts 98, GEMV 84, residual adds 32, K-stage 16, K RoPE-scatter 16. After staged device SwiGLU integration it was 457 launches/token: RMS reductions 147, norm-scale bcasts 98, GEMV 84, SwiGLU 64, residual adds 32, K-stage 16, K RoPE-scatter 16. Sharing FFN RMSNorm across Wgate/Wup brought it to 377 launches/token. Fusing K staging as a RoPE-program preamble brought it to 361 launches/token. Fusing row->footprint into the SiLU program brought it to 345 launches/token. Integrating token embedding gather for layer-0 QKV norm inputs makes the current full 16-layer decode 347 launches/token: RMS reductions 99, norm-scale bcasts 66, GEMV 84, SwiGLU 48, residual adds 32, K-stage+RoPE-scatter 16, embedding 2. Current short full run is 0.96-0.97 tok/s, ~78 ms/token device time and ~1.04 s/token wall time, so staged RMSNorm plus launch/readback/host attention overhead remains the bottleneck.
- [x] **Attention score + softmax + weighted-V proof.** DONE 2026-06-10 (`microbenching/matmul/microbench_attention_scores_softmax.py`, jobs `31442e82`, `0199ea72`, `2c9f74f7`, `ea6e977d`). Proves `q @ K^T -> scores -> softmax(scores) -> probs @ V` on device for `head_dim=64`, `seq=32` using skinny GEMV, softmax composite, and a second skinny GEMV: score+softmax smoke PASS at 2 launches / 15.8 us avg and 100-run soak PASS at 200 launches / 15.4 us avg; full weighted-V smoke PASS at 3 launches / 15.2 us avg and 100-run soak PASS at 300 launches / 15.3 us avg. This is not the integrated attention kernel yet: K/V are supplied in matmul-friendly layouts, the scale is folded into staged K input, and there is no live row-major KV-cache reader or multi-head/GQA driver path. A separate scalar-bcast score-scale stage before softmax repeatedly timed out the following softmax launch (`b2be9f81`, `ae264beb`, `1f5ce859`, verbose `854b7609`); next step is to fold scale into the score writer or isolate the scaled-score layout/state, then wire live K/V cache staging.
- [x] **Attention KV-cache -> GEMV-input staging proof.** DONE 2026-06-10 (`microbenching/matmul/microbench_attention_k_stage.py`, K-only jobs `288d4c6c`/`258eebe4` and `fa731174`/`03ad4cb9`, KV jobs `aae7ca09`/`db26fc4e` and `68180652`/`4f0a486d`, causal jobs `0476309d`/`f17a4bc0` plus edge smokes `61f7e34c`/`37b6d50b`). Separate BRISC/NOC staging programs read one live tilized KV-cache head (`seq=32`, `head_dim=64`): K is transposed into the score GEMV B buffer `K^T[dim, seq]`, V is copied into the weighted-V GEMV B buffer `V[seq, dim]`. The score GEMV, masked-softmax, and weighted-V GEMV programs consume those staged buffers. Current causal path is 5 launches because the causal mask is fused as a BRISC preamble inside the softmax feeder, not as a separate eltwise ADD. Causal KV smoke PASS at pos=17 (5 launches / 17.5 us avg), 100-run soak PASS at pos=17 (500 launches / 17.4 us avg), and edge smokes PASS at pos=0 and pos=31: staged K^T bytes PASS, staged V bytes PASS, scores PASS, causal mask PASS, softmax PASS, weighted V PASS. This keeps the intended attention boundary as a short chain of separate device programs: K^T stage -> score GEMV -> masked softmax -> V stage -> weighted V. Do not force this into one mega-program; only fuse BRISC staging preambles into their neighboring GEMVs after soak and driver integration. Current caveats: unscaled scores in this live-cache proof, only one sequence tile (`seq=32`), no cross-tile softmax, no integrated multi-head/GQA loop yet. Gotcha: a separate eltwise ADD mask before softmax reproduced the old post-ADD softmax CQ timeout (`dcad483b`); keep masking in the softmax BRISC feeder.
- [x] **Attention row-major KV-cache staging for the driver layout.** DONE 2026-06-10 (`microbenching/matmul/microbench_attention_k_stage.py --row-major-cache`, job `1527f804`; full one-head row-major jobs below). The first live-cache proof used a tilized fixture, but `examples/llama3.py` writes K/V caches as raw row-major rows. Added row-major K/V stagers that read 16 cache rows/page and write tilized GEMV inputs. Causal KV chain PASS with staged K^T, staged V, scores, causal mask, softmax, and weighted V all passing. This is the layout the driver integration must use.
- [x] **Attention Q-head staging from QKV C buffer.** DONE 2026-06-10 (`microbenching/matmul/microbench_attention_q_stage.py`, jobs `97927541`, `58e3e3a5`, `1e8e1895`, `996a5610`; failed transient `1f672549`). BRISC/NOC stages one selected Q head from the QKV GEMV output C-buffer into the score GEMV A-buffer, then runs score GEMV. Smoke PASS at 2 launches / 14.5 us avg; 20-run and 60-run soaks PASS; 100-run retry PASS at 200 launches / 14.7 us avg. One earlier 100-run attempt timed out at CQ event 104 but did not reproduce after reset. This proves Q can come from device QKV output without host splitting/readback. Caveat: this is unrotated Q; next bridge is Q RoPE into the same attention A-buffer, then compose Q-stage/Q-RoPE with live K/V staged attention.
- [x] **Attention Q-head staging + RoPE into score GEMV A.** DONE 2026-06-10 (`microbenching/matmul/microbench_attention_q_rope_stage.py`, split jobs `9ebd448e`/`9904061b`, fused jobs `14ffcc47`/`31716052`). BRISC/NOC stages one Q head plus cos/sin into the existing RoPE SFPU footprint, runs RoPE, then custom NCRISC compacts rotated x1/x2 lanes into the score GEMV A-buffer; the following score GEMV consumes that A-buffer directly. Split path PASS at 3 launches / 14.5 us avg and 100-run soak PASS at 300 launches / 14.7 us avg. Fused path folds Q staging into the Q-RoPE BRISC reader and PASSes at 2 launches / 14.7 us avg with 100-run soak PASS at 200 launches / 15.0 us avg. This removes one launch per query head in the integrated attention path.
- [x] **Full staged one-head attention proof.** DONE 2026-06-10 (`microbenching/matmul/microbench_attention_live_head.py`, jobs `e413276c`, `18740d68`, placement-hook jobs `956acf9c`/`0b1c6bae`, edge smokes `9a304318`/`c0bb4f14`; supersedes older `microbench_attention_full_stage.py` jobs `67b218ba`/`de61aa32`). Composes QKV C Q head -> Q RoPE -> score GEMV A; live K cache -> `K^T`; score GEMV; causal masked softmax; live V cache -> `V`; weighted-V GEMV -> context head. The weighted-V output writer now also hooks row 0 directly into the correct full-width `Wo.x_buf` tile slice, so no separate context-copy launch is needed. Smoke PASS at 7 launches / 16.7 us avg and 100-run soak PASS at 700 launches / 16.4 us avg: Q RoPE source PASS, rotated Q A PASS, staged `K^T` PASS, staged V PASS, scores PASS, causal mask PASS, softmax PASS, weighted V PASS, Wo input placement PASS. Edge smokes PASS for q_head=0/kv_head=0,pos=0 and q_head=31/kv_head=7,pos=31. Remaining work is multi-head/GQA wrapping and replacing the host `attention()` call in `examples/llama3.py`.
- [x] **Full staged one-head attention proof against row-major driver caches.** DONE 2026-06-10 (`microbenching/matmul/microbench_attention_live_head.py --row-major-cache`, jobs `2caf18eb`, `fe9d8218`, edge smokes `cce4e98c`/`d4e4f761`). Same QKV C Q head -> Q RoPE -> score GEMV A -> masked softmax -> weighted V -> direct Wo input chain, but K/V are now raw row-major cache pages matching `examples/llama3.py`. Smoke PASS at 7 launches / 18.1 us avg, 100-run soak PASS at 700 launches / 17.9 us avg, and pos/head edges PASS.
- [x] **Opt-in device attention integration in `examples/llama3.py`.** DONE 2026-06-10 (`--device-attention`, split jobs `898148fc`/`fb87f249`, fused-Q jobs `2af2abe6`/`a3622288`, fused-Q+KV jobs `3eea8a58`/`62f53052`, full-model fused-Q `f8bf8ab0`; baseline `07fbbc15`). The driver skips host QKV readback/host `attention()` when the flag is enabled, runs the staged attention chain over all heads, writes directly into `Wo.x_buf`, and calls `Wo.run_prefilled()`. K/V are staged once per KV head and reused across the GQA group; after fused Q-stage+Q-RoPE and fused row-major K/V staging, attention is 136 launches/layer rather than 176. One-layer default-prompt pos=5 fused-Q+KV smoke PASS: 168 total launches/token, attention 136 launches, 3.17 tok/s; one-layer 4-token pos=5..8 relaunch PASS at ~3.19 tok/s. Full 16-layer fused-Q smoke PASS but is launch-heavy: 2651 launches/token, attention 2304, 0.23 tok/s, 114 ms device time / 4.4 s wall. Caveats: opt-in only, one sequence tile (pos 0..31), unscaled scores, host prefill still computes on host then copies KV to device, pos=0 one-token driver smoke currently times out in K-stage/RoPE despite isolated K updater pos=0 PASS (`1b9526f0`).
- [x] **GQA-group score/softmax/weighted-V path.** DONE/PARTIAL 2026-06-10 (`microbenching/matmul/microbench_attention_gqa_group.py`, `examples/llama3.py --device-attention`). One KV group first ran as four fused Q-stage/RoPE launches, one Q-row assembler, one fused row-major K/V stage, one grouped score GEMV, one four-row masked softmax, and one grouped weighted-V-to-Wo writer: 9 launches/group instead of the previous 17/group. Standalone edge smoke PASS at pos=31 (`eb87f89d`, 9 launches / 18.7 us avg) and 100-run soak PASS (`5662f914`, 900 launches). Driver one-layer checked smoke PASS (`2b623335`, 104 launches/token, attention 72) and full 16-layer one-token smoke PASS (`c0f5e5be`, 1499 launches/token, attention 1152, 0.38 tok/s), down from 2651 launches/token / attention 2304.
- [x] **Grouped Q/RoPE for one GQA group.** DONE/PARTIAL 2026-06-10 (`microbenching/matmul/microbench_attention_q_rope_stage.py`, `microbenching/matmul/microbench_attention_gqa_group.py`, `examples/llama3.py --device-attention`). Added `build_q_rope_group_stage_to_a_program`: one launch feeds four Q heads through the existing TRISC RoPE path and NCRISC compacts rows 0..3 directly into the grouped score-GEMV A buffer. Standalone grouped-Q GQA proof PASS (`bab9024a`, 5 launches / 22.6 us avg); edge smokes PASS (`109bf359` pos=0, `3be7312f` pos=31); 100-run/500-launch soak PASS (`99e85d62`, 22.8 us avg). Grouped Q A rows, staged K^T/V, scores, causal mask, softmax, weighted V, and Wo placement all PASS. Driver one-layer smoke PASS (`bc716a67`, 72 launches/token, attention 40, 5.79 tok/s). Full 16-layer one-token smoke PASS (`aed01ad8`, 987 launches/token, attention 640, 0.47 tok/s, 92.5 ms device time). Removed the per-layer host zero-fill of `Wo.x_buf` before device attention because all row-0 head slices are overwritten before Wo GEMV. Checked one-layer four-token decode PASS (`739d9bb4`, argmax ok, 72 launches / 22 submits per token); full 16-layer four-token decode PASS (`f257dee4`, 987 launches / 307 submits per token, 0.54 tok/s); post-reset full 16-layer 16-token decode PASS (`b2415296`, pos 5..20, 0.54 tok/s). A pre-reset 16-token attempt timed out in `wdown_residual_add` (`800c0017`, event 2196), so longer soak is still required before calling this stable long-running.
- [x] **Resident projection residual sources + CQ batching.** DONE/PARTIAL 2026-06-10 (`examples/llama3.py`). `DeviceResidualGemv` can use row-tiled device activation buffers as residual sources: `Wo` residual add consumes the QKV norm input row buffer, and `Wdown` residual add consumes the Wgate norm input row buffer. This removes two residual-vector host uploads per layer/token. Also removed host zeroing of RMS `partial1` buffers because the K=2048 compact reduction fully overwrites those two tiles; `partial2` zeroing remains required. Program submission batching now groups GEMV chunks, RMSNorm+scale+GEMV chains, each 5-program attention group, projection+residual-add chains, the 3-program SwiGLU bridge, and the 2-program embedding bridge. Checked one-layer decode PASS (`982ac893`, argmax ok, 72 launches / 17 submits per token). Full 16-layer 16-token decode PASSes twice back-to-back (`2bd62082`, `7c2f925a`, pos 5..20, 987 launches / 242 submits per token, 0.57 tok/s). One max-first-tile run (`cf5786f8`, steps=27) reached pos 23, then timed out in a later QKV RMS/GEMV submission (`event 8121`); after reset, a current-tree max-first-tile run PASSed through pos 31 (`fc6009b2`, 27 generated tokens, 987 launches / 242 submits per token, 0.57 tok/s). This is a real reduction in host traffic/submission pressure, but cross-tile and longer generation stability remain open.
- [x] **Resident activation handoff into later norms/logits.** DONE/PARTIAL 2026-06-10 (`examples/llama3.py`). Later layer QKV RMSNorms now consume the previous layer's `Wdown.next_buf`, Wgate RMSNorms consume the current `Wo.next_buf`, and tied logits consumes the final `Wdown.next_buf`. This keeps decode activations device-resident through the layer stack in `--device-attention` mode, except logits readback for host argmax. The device-attention decode path also skips the unused host embedding row and host RoPE table row lookup. It adds row-tile-to-RMS-footprint converters, so launch count rises to 1019, but removes the remaining host activation upload boundary for those norms. Checked one-layer decode PASSes (`f5025d8d`, `19340d80`, current-tree `27c1f3df`, 74 launches / 17 submits per token, argmax ok). Full 16-layer one-token decode PASS (`db728ebe`, current-tree `6e15f26b`, 1019 launches / 242 submits per token, 0.62 tok/s). Current-tree 16-token decode PASS (`ccad6c63`, pos 5..20, 0.62 tok/s). Full first-tile run PASS (`9efa0f30`, 27 generated tokens through pos 31, 1019 launches / 242 submits per token, 0.62 tok/s). Remaining host-side pieces: prompt prefill/cache sync and logits readback/argmax; argmax is explicitly allowed for now.
- [x] **Grouped score GEMV with synchronized K/V-stage preamble.** DONE/PARTIAL 2026-06-10 (`microbenching/matmul/microbench_attention_gqa_group.py --fuse-kv-score`, `examples/llama3.py`). Each GQA group now runs as four programs instead of five: grouped Q/RoPE, score GEMV with NCRISC K/V-stage preamble and BRISC semaphore wait, masked softmax, weighted-V-to-Wo. BRISC-only staging raced the score GEMV's B reader and produced zero scores; unsynchronized NCRISC staging raced BRISC's matmul CB use. Synchronized proof PASS (`89d6450f`, 4 launches/group, 25.5 us avg) and 100-run soak PASS (`84b7c7e3`, 400 launches, 25.9 us avg). Driver checks PASS: one-layer checked decode (`968b0c9a`, current-tree `149a3f14`, 66 launches / 17 submits per token, argmax ok), repeated full 16-layer one-token decode (`8b4640e3`, `a6b232bd`, current-tree `3084cf97`, 891 launches / 242 submits per token, 0.62-0.63 tok/s), full 16-layer 16-token breakdown (`a944d11d`, current-tree soak `883d3f8d`, pos 5..20, 891 launches / 242 submits per token), full 16-layer first-tile decode (`70304c02`, 27 generated tokens through pos 31, 891 launches / 242 submits per token, 0.63 tok/s), and one-layer cross-tile decode through pos 44 (`ace1d38a`, 40 generated tokens, 66 launches / 17 submits per token, 7.29 tok/s). This saves 128 launches/token on the full 16-layer path while keeping score, softmax, and weighted-V as separate stable programs. Follow-up score-scale cleanup folds Llama's `1/sqrt(64)=1/8` into row-major K staging (`--k-scale-pow2-down 3`) instead of scaling inside the softmax feeder; fused row-major K/V smoke PASS (`05c16415`, 4 launches, 28.7 us avg), 100-run soak PASS (`74150b38`, 400 launches, 28.8 us avg), grouped GQA proof PASS (`04d22bf1`, fused KV-score preamble, `k_scale=2^-3`, 20 runs / 80 launches), one-layer checked decode PASS (`99b4588b`, repeated by `8551956e`, argmax ok), full 16-layer one-token PASS (`3061047a`, repeated by `17c40912`, 891 launches / 242 submits per token), and full 16-layer four-token breakdown PASS (`b03b31a4`, 891 launches / 242 submits per token, ~99 ms device time/token, 0.22 tok/s wall). A 16-token full run (`40fa75e2`) emitted all decode tokens through pos20 at 891 launches / 242 submits and ~99 ms device time/token, then hit the 300 s queue wall timeout before clean process exit. Cross-tile remains block-local attention, not full historical attention.
- [x] **Multi-tile grouped attention score proof.** DONE/PARTIAL 2026-06-10 (`microbenching/matmul/microbench_attention_gqa_multitile_scores.py`). Proves the first full-history building block without changing the integrated decode path: grouped Q/RoPE runs once, then each live 32-token history tile runs a separate synchronized KV-stage+score GEMV preamble and writes its grouped score tile on device. A tiny current-tile causal score-mask program then writes the bf16 `-100` softmax sentinel into columns after `pos % 32`; previous history tiles remain unmasked. Pre-mask score tiling PASSes (`dbd58a44`, `ee1dce44`, `6f01a959`). Masked score smokes PASS (`aad41a8d`, `f388379e`, `9efe0968`). The shared row-major K/V stager now also has explicit destination sequence placement (`dst_seq_start`) and partial-page writeback for prefix staging; lower-level two-tile score-only proof PASSes (`a77ab68b`, 3 launches) and 100-run soak PASSes (`cbff2b33`, 300 launches, 51.3 us avg), with scaled K^T, V, and scores all validating across a 64-token prefix. A BRISC-only global row-max pass now reads the per-tile bf16 score buffers, compares all live columns with an order-preserving bf16 integer key, and writes the row max tile on device. Rowmax two-live-tile smoke PASS (`d72f6b24`, pos47, 5 launches), 20-run/two-tile soak PASS (`232cb811`, 100 launches, 41.5 us avg), and three-live-tile edge PASS (`07e4a642`, pos95, 6 launches, 47.8 us avg). A separate SFPU per-tile softmax-stats producer (`microbenching/matmul/microbench_attention_global_softmax_stats.py`) computes each tile's row max and `sum(exp(score - row_max))`; 2-tile smoke PASS (`6b040f67`), 2-tile 100-run soak PASS (`af1e49d5`), and 3-tile edge PASS (`317f6f13`). The current global-stat pipeline (`microbenching/matmul/microbench_attention_global_softmax_combine.py`) runs score tiles -> per-tile stats -> compact stats tile -> SFPU global combiner, computing `global_max=max(tile_max)` and `global_sum=sum(tile_sum*exp(tile_max-global_max))` without host arithmetic; current smokes PASS for 2 tiles (`b17216de`, 3 launches, 15.0 us avg), 3 tiles (`760ae08d`, 3 launches, 14.4 us avg), and a 2-tile 100-run soak (`4d735529`, 300 launches, 14.6 us avg). Probability scaling from global stats is now proved by `microbenching/matmul/microbench_attention_global_softmax_probs.py`: current 2-tile smoke PASS (`086ccd25`, 4 launches, 15.5 us avg), 2-tile 100-run soak PASS (`b6025f1d`, 400 launches, 14.7 us avg), and 3-tile edge PASS (`e73a16aa`, 4 launches, 14.7 us avg), with probability rows summing near 1 and non-logical rows cleared for downstream GEMV safety. Weighted-V accumulation across live history tiles is proved conservatively by `microbenching/matmul/microbench_attention_multitile_weighted_v.py`: current 2-tile smoke PASS (`de85988f`, 3 launches, 14.9 us avg), 2-tile 100-run soak PASS (`26096a12`, 300 launches, 14.5 us avg), and 3-tile edge PASS (`7c7c4280`, 5 launches, 14.2 us avg). The full-history chain is now integrated in `examples/llama3.py` for `pos >= 32` when `--device-attention` is used without `--allow-block-local-attention`; one-layer checked decode crosses into the second tile at pos32 with argmax ok (`8b3a9cd2`, pos32 = 162 launches / 17 submits, attention 128 launches). A one-GEMV-across-history optimization was probed in `microbench_attention_global_softmax_probs.py --weighted-v`: host-written GEMV-A diagnostic PASSes (`28863b33`), but the device probability-tile-list -> GEMV-A compactor is not correct yet (`580a60b8`), so keep the stable per-tile GEMV + ADD accumulator path. Q rows, multi-tile causal scores, current-tile causal mask, global row max, per-tile exp-sum stats, compacted stats, global denominator stats, per-tile probability scaling, weighted-V context accumulation, and one-layer driver handoff all validate as separate device programs. Remaining full-history attention work: soak full-model decode past the first tile and then longer sequences. Keep the full-history path as a short chain of stable programs; possible later fusions are current-tile mask into stats production and probability-scaler output directly into a GEMV-A layout, not a single attention mega-program.
- [ ] **Resident row-to-RMS conversion fused into RMS sumsq.** EXPERIMENTAL/GATED 2026-06-10 (`examples/llama3.py --fuse-row-to-rms`, `microbenching/tensix/microbench_rmsnorm_inv.py`, `microbenching/matmul/microbench_swiglu_bridge.py`). Resident row-buffer RMS inputs can fold row-tile-to-RMS-footprint conversion into the first RMS sumsq reduction as a BRISC preamble. The layer-0 token embedding gather now routes through the same `DeviceNormGemv` row-source mechanism, so the embedding row-to-RMS conversion also disappears under `--fuse-row-to-rms` instead of living as a standalone bridge. Default RMS reducer smoke PASS (`95c1f7f1`), fused one-layer checked decode PASS (`9fa1c912`, 64 launches / 17 submits per token, argmax ok), embedding/RMS-routed fused one-layer checked decode PASS (`2aa78c0c`, 63 launches / 17 submits per token, argmax ok), full 16-layer one-token PASS (`18f1eb33`, 859 launches / 242 submits per token, 0.62 tok/s), and focused full-model pos16 retry PASS (`349f8e9b`, 12 generated tokens, 0.63 tok/s). Two 16-token attempts around this change timed out in layer-7 attention (`e6c841d0` at pos16, `674189af` at pos9), so keep it off by default until it soaks cleanly. Stable default remains the separate row-to-RMS converter at 891 launches/token; one-layer default check PASSes (`e100d8f6`, 66 launches / 17 submits per token, argmax ok). A later default 16-token run also timed out in attention (`88b73829`), so long-soak attention/CQ stability remains open independent of this gated fusion.
- [ ] **Carry device-attention decode past the first tile.** NEXT. The older fused-score path survived the first decode tile (`pos <= 31`) at 891 launches / 242 submits per token and had block-local cross-tile debug runs, but that was not model-correct. The current `examples/llama3.py --device-attention` path now switches to full-history attention at `pos >= 32` unless `--allow-block-local-attention` is passed. One-layer checked decode first crossed into the second tile and matched host argmax at pos32 (`8b3a9cd2`, attention 128 launches, 162 total launches / 17 submits). The current local-fusion path keeps separate programs but removes the score-copy and probability-copy launches: score GEMV mirrors into the full-history score list from its output writer, current-tile masking edits that history page in place, and weighted-V reads probability pages directly via separate A/C matmul layout offsets. Updated one-layer checked decode PASSes at pos32 (`5ecefba0`, attention 96 launches, 130 total launches / 17 submits, argmax ok). Full 16-layer decode reached pos32 before this launch reduction (`98514a05`): pos5..31 stay on the compact first-tile path at 891 launches / 242 submits and ~0.21-0.22 tok/s, then pos32 runs model-correct two-tile attention at 2427 launches / 242 submits, attention 2048 launches, ~126.6 ms device time and 0.12 tok/s wall. A longer full-model attempt (`01f38d7b`) timed out at pos21 in `l5.wup_c0`, before full-history attention, so compact-path/CQ stability is still open. Eager full-history program construction made full-model init slow (128.9s in the `98514a05` run), so the next engineering target is lazy/full-history-on-demand program construction plus longer soaks past pos32. Separate programs are expected: one program per live score tile is acceptable, and stats/denominator/V-accumulation should stay split until each boundary soaks cleanly.
- [x] **All-head staged attention -> Wo input proof.** DONE 2026-06-10 (`microbenching/matmul/microbench_attention_all_heads_wo.py`, jobs `19fa278a`, `41ef855b`, `6ca5bfb2`, edge smokes `99b2698d`/`734612a6`). Wraps the live-cache attention chain over all 32 Q heads / 8 KV heads, writes each weighted-V row directly into the correct `Wo.x_buf` slice, checks padded K columns and padded rows stay zero, then consumes the assembled device buffer with a real Wo GEMV. N=256 smoke PASS (225 launches / 16.3 us avg), full-width N=2048 smoke PASS (225 launches / 18.7 us avg), 10-run N=256 relaunch soak PASS (2250 launches / 16.1 us avg), and causal edge smokes PASS at pos=0 and pos=31, with Wo GEMV PCC ~0.99995 / rel_l2 ~0.011. This removed the separate context-copy launch from the attention integration plan; driver integration and Q-stage+Q-RoPE fusion are now covered above.
- [x] **KV-cache indexed scatter (llama plan #7, write half).** DONE 2026-06-10 (jobs `1490704e` max_seq=64, `591133ef` max_seq=256). BRISC-only kernel: noc_read pos's staged page → L1 scratch, then 16× 128 B noc_writes at `base + (kv_head*max_seq+pos)*128` with page→bank math in registers; `pos` per launch via RTA, full shuffled pos sweep. K+V caches byte-exact; ~14–15 µs/launch (launch-overhead bound — 2 KB payload is noise). Cache stays row-major in the standard 2 KiB-page interleave; rows never straddle pages (max_seq % 16 == 0). EMU=1 broken for ALL benches: launch-path tensix state reset hits unimplemented `tensix_cfg_wr32 reg=4`.

## Process

Device wedges: ARC `boot_status=0xffffffff`, core-(1,2) waits, PCIe drop (cold boot only). Quarantined runs only via queue, build-only first, one short job, stop on first anomaly.
